<Window x:Class="RDT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RDT"
        xmlns:Models="clr-namespace:RDT.Models"
        xmlns:vm="clr-namespace:RDT.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1010" WindowStyle="None"
        ResizeMode="NoResize" Background="Transparent" AllowsTransparency="True"        
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown"       
        >
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Border Background="#FF212020"
            CornerRadius="5" >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border  Grid.Row="0" Grid.Column="0"
                     Background="#FF2196F3">
                <TextBlock
                       Text="RDT"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Foreground="#FFFCFEFF"
                       FontSize="22"
                       FontFamily="Candara Light"
                       Margin="50,0,0,0"/>
            </Border>
            
            <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" FlowDirection="RightToLeft"  Width="100" HorizontalAlignment="Right">
                <Button x:Name="BtnClose" Width="25" Height="25"  VerticalAlignment="Top" Click="BtnClose_Click" Background="Transparent"/>
                <Button x:Name="BtnMaximaize" Width="25" Height="25" VerticalAlignment="Top" Click="BtnMaximaize_Click" Background="Transparent"/>
                <Button x:Name="BtnMinimize" Width="25" Height="25" VerticalAlignment="Top" Click="BtnMinimize_Click" Background="Transparent"/>
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Top" Orientation="Horizontal">
                <RadioButton Content="Home" Margin="15,0"
                             Height="60" Width="200"                            
                             Style="{StaticResource MenuButton}"
                             Command="{Binding HomeViewCommand}"
                             IsChecked="True"
                             />
                <RadioButton Content="Credentials" Margin="15,0"
                             Height="60" Width="200"                       
                             Style="{StaticResource MenuButton}"
                             Command="{Binding UserCredViewCommand}"
                             />

            </StackPanel>
            <TreeView  Grid.Row="1" Grid.Column="0" x:Name="treeView" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=GeneralList}" Foreground="LightGray" Background="Transparent" FontSize="11" Padding="0,20,0,0">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type Models:Category}" ItemsSource="{Binding Members}">
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding Path=(Models:ItemHelper.IsChecked), Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <CheckBox.Style>
                                <Style TargetType="{x:Type CheckBox}">
                                    <Setter Property="Foreground" Value="LightGreen"/>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=(Models:ItemHelper.IsChecked)}" Value="False">
                                            <Setter Property="Foreground" Value="LightGray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </HierarchicalDataTemplate>
                    <DataTemplate DataType="{x:Type Models:Env}" >
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding Path=(Models:ItemHelper.IsChecked), Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <CheckBox.Style>
                                <Style TargetType="{x:Type CheckBox}">
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Path=(Models:ItemHelper.IsChecked)}" Value="False">
                                            <Setter Property="Foreground" Value="LightGray"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </DataTemplate>
                </TreeView.Resources>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}">
                        <Setter Property="IsExpanded" Value="True"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>

            <ContentControl Grid.Row="1"
                            Grid.Column="1"
                            Margin="0,0,10,20"
                            Content="{Binding CurrentView}" Grid.ColumnSpan="2"/>


        </Grid>
    </Border>
</Window>
